package at.ac.fhcampuswien.coffescript.Crawler;

import java.util.Comparator;

class mmmmmcCompareTo implements Comparable<mmmmmC> {
//TODO Override anpassen damit temp.add(test); in mmmmmcManager funktioniert
    @Override
    public int compareTo(mmmmmC o) {
        return 0;
        //return URL.compareTo(o.URL);
    }
}
